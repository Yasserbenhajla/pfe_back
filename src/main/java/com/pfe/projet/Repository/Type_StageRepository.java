package com.pfe.projet.Repository;

import com.pfe.projet.Entity.Type_Stage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Type_StageRepository extends JpaRepository<Type_Stage,Long> {
}
